Here is the complete dependency picture for your refactoring task.

---

## Source File Under Consideration

There is exactly **one file** in the ledger directory:

**`/home/visiona/workspace/mana-hive/hub/hub-service/src/main/kotlin/com/manahive/hub/ledger/LedgerPort.kt`** (33 lines)

It defines four public types, all in a single file:
- `LedgerPort` (interface) -- line 10
- `AppendResult` (sealed interface) -- line 16
- `StoredEvent` (data class) -- line 22
- `WatermarkPort` (interface) -- line 30

Its only Kotlin import is `com.manahive.contracts.EventEnvelope`, which already lives in the `platform/contracts` module.

---

## All References Found Across the Codebase

### 1. `.kt` files (source code)

| Type | File | Line | Context |
|------|------|------|---------|
| `package com.manahive.hub.ledger` | `hub/hub-service/src/main/kotlin/com/manahive/hub/ledger/LedgerPort.kt` | 1 | Package declaration |
| `LedgerPort` | `hub/hub-service/src/main/kotlin/com/manahive/hub/ledger/LedgerPort.kt` | 10 | Interface definition |
| `AppendResult` | `hub/hub-service/src/main/kotlin/com/manahive/hub/ledger/LedgerPort.kt` | 11, 16-19 | Usage in `append()` return type + definition |
| `StoredEvent` | `hub/hub-service/src/main/kotlin/com/manahive/hub/ledger/LedgerPort.kt` | 12, 13, 22 | Usage in `read()`/`readFrom()` return types + definition |
| `WatermarkPort` | `hub/hub-service/src/main/kotlin/com/manahive/hub/ledger/LedgerPort.kt` | 30 | Interface definition |

**No other `.kt` file in the entire repository imports or references `com.manahive.hub.ledger`, `LedgerPort`, `WatermarkPort`, `StoredEvent`, or `AppendResult`.** A grep for `import com.manahive.hub.ledger` returned zero results outside the file itself. A grep for `hub\.ledger` in `*.kt` files returned only the package declaration on line 1.

### 2. `.gradle.kts` files (build scripts)

**No references whatsoever.** No Gradle file mentions `hub.ledger`, `LedgerPort`, `WatermarkPort`, `StoredEvent`, or `AppendResult`.

### 3. Documentation / Markdown files

| Type | File | Line | Context |
|------|------|------|---------|
| `LedgerPort` | `/home/visiona/workspace/mana-hive/files/11-acta-workspace-real.md` | 59 | Sprint 1 planning doc: _"Los puntos de partida estan marcados en el codigo: `LedgerPort` (hub)..."_ |
| `StoredEvent` | `/home/visiona/workspace/mana-hive/docs/design/hub-scene-event-handler.md` | 124 | Rust pseudocode example: `let stored_event = StoredEvent { ... }` (different type, different language) |

These are purely documentary references with no code-level coupling.

### 4. YAML / JSON / XML / properties / TOML files

**Zero references.**

### 5. Test files

**There are no test files under `hub/hub-service/src/test/` at all.** The directory either does not exist or is empty. No test file anywhere in the repository references any of the four types.

---

## Target Module Analysis

### `platform/contracts/build.gradle.kts`

```kotlin
plugins { id("manahive.pure-domain") }

dependencies {
    api(projects.platform.domainKernel)
}
```

**Key observations:**
- Uses the `manahive.pure-domain` convention plugin, which enforces **purity**: no external (non-Kotlin-stdlib) dependencies allowed on the runtime classpath. This is enforced by the `verifyPurity` task in `build-logic/src/main/kotlin/manahive.pure-domain.gradle.kts` (line 13-25).
- The only declared dependency is `platform/domain-kernel` (which itself is a pure module with zero dependencies).
- The target directory `platform/contracts/src/main/kotlin/com/manahive/contracts/ledger/` does **not yet exist** -- it must be created.

### Existing files in `platform/contracts`

```
EventEnvelope.kt          (root package)
alarm/AlarmEvent.kt
census/CensusSnapshot.kt
perception/Observation.kt
policy/EffectiveRules.kt
scene/SceneFact.kt
scene/PersonState.kt
sentinel/SentinelSignal.kt
```

No `ledger/` subdirectory exists yet.

### `hub/hub-service/build.gradle.kts`

```kotlin
plugins { id("manahive.spring-service") }

dependencies {
    implementation(projects.hub.hubDomain)
    implementation(projects.platform.messaging)
    implementation("org.springframework.boot:spring-boot-starter-jdbc")
    runtimeOnly(libs.postgres)
    testImplementation(libs.testcontainers.postgres)
}
```

Notably, **`hub-service` does not declare a dependency on `platform/contracts`**, yet `LedgerPort.kt` imports `com.manahive.contracts.EventEnvelope`. This dependency is likely satisfied transitively (through `hub.hubDomain` or `platform.messaging`). After the move, `hub-service` will need to depend on `platform/contracts` (or it already does transitively) to keep using the types.

---

## Summary of Impact

| Aspect | Finding |
|--------|---------|
| **Source consumers in `.kt` files** | **None.** No Kotlin file outside `LedgerPort.kt` imports any of the four types. The move has zero compile-time breakage risk in existing code. |
| **Build script references** | **None.** No `.gradle.kts` file references the old package. |
| **Test consumers** | **None.** No test files exist for these types (or any tests at all under `hub-service`). |
| **Documentation** | Two markdown files mention `LedgerPort`/`StoredEvent` by name but have no code-level coupling. |
| **Circular dependency risk** | **None.** The only import in `LedgerPort.kt` is `EventEnvelope`, which already lives in `platform/contracts`. Moving the file there means it will reference a sibling type within the same module -- no new dependency edges are introduced. |
| **Purity constraint** | **Compatible.** The types being moved are pure interfaces and data classes with no framework annotations or external imports beyond `EventEnvelope` and Kotlin stdlib types (`Long`, `String`, `List`). They satisfy the `manahive.pure-domain` contract. |
| **New build dependency needed** | After the move, any module that uses `LedgerPort`/`WatermarkPort`/etc. will need `implementation(projects.platform.contracts)` (or already has it transitively). Currently no module imports them, so no build file changes are required immediately. |